# Maquettes

`vision.html` + `support.js` — toutes les pages, navigation gérée en JS.
Référence visuelle uniquement : ne pas brancher, ne pas modifier.
En cas de contradiction, `design-system.md` fait autorité sur les valeurs
visuelles, et `brief-design.md` sur la structure et le contenu.
Pas de fichier de tokens : ils seront extraits du CSS en T1.1.